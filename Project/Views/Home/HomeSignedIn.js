import * as React from 'react';
import { View } from 'react-native';
import { Layout } from '../../Theme/Layout';

export default function HomeSignedIn({ navigation }) {
  return (
    <View style={Layout.container}>
      
    </View>
  );
}
